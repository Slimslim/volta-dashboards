# volta-dashboards

Public host for VOLTA dashboard embeds (Notion /embed targets) — counts-only, self-contained pages.

**Source of truth is the private Project-Volta repo** (`docs/dashboards/`). Files here are mirrored automatically by CI — edit them there, not here.
